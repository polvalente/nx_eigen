name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-linux:
    name: Test on Linux (x86_64)
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        elixir: ["1.18.1"]
        otp: ["27.2"]
    env:
      MIX_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ matrix.otp }}
          elixir-version: ${{ matrix.elixir }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libfftw3-dev \
            libfftw3-3

      - name: Verify FFTW installation
        run: |
          pkg-config --exists fftw3 fftw3f || (echo "FFTW not found!" && exit 1)
          pkg-config --libs fftw3 fftw3f
          ldconfig -p | grep fftw || true

      - name: Restore Eigen cache
        uses: actions/cache@v4
        with:
          path: eigen-3.4.0
          key: eigen-3.4.0

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-${{ matrix.otp }}-${{ matrix.elixir }}-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Check formatting
        run: mix format --check-formatted

      - name: Run tests
        env:
          LD_LIBRARY_PATH: /usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
        run: mix test

  # The Eigen FFT backend is what the Nerves target ships, and no runner can
  # execute that cross-compiled artifact, so exercise the backend here instead.
  test-linux-eigen-fft:
    name: Test on Linux (x86_64, Eigen FFT)
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        elixir: ["1.18.1"]
        otp: ["27.2"]
    env:
      MIX_ENV: test
      NX_EIGEN_FFT_LIB: eigen

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ matrix.otp }}
          elixir-version: ${{ matrix.elixir }}

      # No FFTW: this build must not need it.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: Restore Eigen cache
        uses: actions/cache@v4
        with:
          path: eigen-3.4.0
          key: eigen-3.4.0

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      # `make` compares the .so against its sources, so a cached or stale
      # artifact would be tested instead of an Eigen-FFT build. Start clean.
      - name: Compile (warnings as errors)
        run: |
          rm -f priv/libnx_eigen.so
          mix compile --warnings-as-errors --force

      - name: Verify the Eigen FFT backend was built
        run: |
          readelf -d _build/test/lib/nx_eigen/priv/libnx_eigen.so | grep NEEDED

          if readelf -d _build/test/lib/nx_eigen/priv/libnx_eigen.so | grep -q fftw; then
            echo "ERROR: build links FFTW despite NX_EIGEN_FFT_LIB=eigen"
            exit 1
          fi

      - name: Run tests
        run: mix test

  test-linux-aarch64:
    name: Test on Linux (aarch64)
    runs-on: ubuntu-22.04-arm
    strategy:
      matrix:
        elixir: ["1.18.1"]
        otp: ["27.2"]
    env:
      MIX_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ matrix.otp }}
          elixir-version: ${{ matrix.elixir }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libfftw3-dev \
            libfftw3-3

      - name: Verify FFTW installation
        run: |
          pkg-config --exists fftw3 fftw3f || (echo "FFTW not found!" && exit 1)
          pkg-config --libs fftw3 fftw3f
          ldconfig -p | grep fftw || true

      - name: Restore Eigen cache
        uses: actions/cache@v4
        with:
          path: eigen-3.4.0
          key: eigen-3.4.0

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-aarch64-mix-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-aarch64-mix-${{ matrix.otp }}-${{ matrix.elixir }}-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Check formatting
        run: mix format --check-formatted

      - name: Run tests
        env:
          LD_LIBRARY_PATH: /usr/lib/aarch64-linux-gnu:$LD_LIBRARY_PATH
        run: mix test

  test-macos:
    name: Test on macOS (ARM64)
    runs-on: macos-14
    env:
      MIX_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        run: |
          brew install erlang elixir fftw pkg-config
          mix local.hex --force
          mix local.rebar --force

      - name: Restore Eigen cache
        uses: actions/cache@v4
        with:
          path: eigen-3.4.0
          key: eigen-3.4.0

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-arm64-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-arm64-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Check formatting
        run: mix format --check-formatted

      - name: Run tests
        run: mix test

  test-macos-x86:
    name: Test on macOS (x86_64)
    runs-on: macos-15-intel
    env:
      MIX_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        run: |
          brew install erlang elixir fftw pkg-config
          mix local.hex --force
          mix local.rebar --force

      - name: Restore Eigen cache
        uses: actions/cache@v4
        with:
          path: eigen-3.4.0
          key: eigen-3.4.0

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-x86-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-x86-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Check formatting
        run: mix format --check-formatted

      - name: Run tests
        run: mix test
